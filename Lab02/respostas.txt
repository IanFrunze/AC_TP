1.
    O programa inicializa as variáveis var1, var2 e var3, correspondente a 0xAC, 126 e 0x2022, respetivamente.
    O programa começa por guardar o endereço da var2 no registo 0. Em seguida, inicia um ciclo loop, 
    onde é incrementado 1 ao valor var2 situado no registo 1, sendo o resultado guardado na memória onde se 
    originalmente encontrava a variável var2. Como o programa executa um branch incondicional para si mesmo no 
    fim de cada ciclo, o loop corre indefinidamente.

2. 
    18

3.
    Cada instrução ocupa 1 ciclo máquina, com a exceção das instruções LOAD (ldr, ldrb) e STORE (srt, strb) que ocupam 2 ciclos máquina. 
    Como cada ciclo máquina corresponde a 3 ciclos clock, o número de ciclos de clock gastos na execução de cada iteração do ciclo loop é 18. 
    (6 ciclos máquina x 3 ciclos clock por = 18 ciclos clock no total)

4.
    var2_addr	0x000A 
    var1	0x000C
    var2	0x000D
    var3	0x000E


5.
    ldr r0, var2_addr 0x0000 0x0C40
    ldrb    r1, [r0, #0] 0x0002 0x0801
    add     r1, r1, #1 0x0004 0xA091
    strb    r1, [r0, #0] 0x0006 0x2801
    b loop 0x0008 0x5BFC